{
  "name": "ci-cd-demo",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny, presentation-ready CI/CD demo with zero runtime dependencies.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs"
  }
}
